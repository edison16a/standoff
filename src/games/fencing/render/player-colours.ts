import type { Slot } from "@/games/fencing/players";

/** Red and green, like the scoring lamps on a real strip. */
export const PLAYER_COLOURS: Record<Slot, number> = { 1: 0xff4757, 2: 0x2ed573 };

/** The party colours: bunting, confetti and the crowd's shirts. */
export const FESTIVE = [0xff4757, 0x2ed573, 0xffd23f, 0x3a86ff, 0xff7ad9, 0xff9f1c];
